# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

include: package:lints/recommended.yaml

linter:
  # The lint rules applied to this project can be customized in the

  # section below to disable rules from the included above or to enable additional rules.
  # A list of all available lints and their documentation is published at https://dart.dev/lints.
  #
  # Instead of disabling a lint rule for the entire project in the
  # section below, it can also be suppressed for a single line of code
  # or a specific dart file by using the `// ignore: name_of_lint` and
  # `// ignore_for_file: name_of_lint` syntax on the line or in the file
  # producing the lint.
  rules:
    # --- BEGIN RECOMMENDED ADDITIONAL LINTS FOR PACKAGES ---
    # These often help with maintainability, consistency, and clarity.
    avoid_empty_else: true
    avoid_function_literals_in_foreach_calls: true
    avoid_init_to_null: true
    avoid_returning_null_for_void: true
    avoid_unused_constructor_parameters: true
    avoid_void_async: true
    camel_case_extensions: true
    unused_field: ignore
    camel_case_types: true
    cast_nullable_to_non_nullable: true
    control_flow_in_finally: true
    empty_catches: true
    empty_constructor_bodies: true
    empty_statements: true
    file_names: true
    hash_and_equals: true
    iterable_contains_unrelated_type: true
    library_private_types_in_public_api: true
    no_leading_underscores_for_local_identifiers: true
    non_constant_identifier_names: true
    null_check_on_nullable_type_parameter: true
    null_closures: true
    prefer_const_constructors: true
    prefer_const_declarations: true
    prefer_contains: true
    prefer_final_fields: true
    prefer_final_locals: true
    prefer_for_elements_to_map_fromIterable: true
    prefer_if_null_operators: true
    prefer_initializing_formals: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_single_quotes: true # Enforces single quotes for string literals.
    sized_box_for_whitespace: true
    sort_child_properties_last: true
    type_annotate_public_apis: true
    unnecessary_brace_in_string_interps: true
    unnecessary_const: true
    unnecessary_late: true
    unnecessary_new: true
    unnecessary_parenthesis: true
    unnecessary_null_aware_assignments: true
    unnecessary_overrides: true
    unnecessary_raw_strings: true
    unnecessary_this: true
    use_full_hex_values_for_flutter_colors: true
    use_key_in_widget_constructors: true
    use_named_constants: true
    use_rethrow_when_possible: true
    use_setters_to_change_properties: true
    # --- END RECOMMENDED ADDITIONAL LINTS FOR PACKAGES ---
# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
